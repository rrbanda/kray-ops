apiVersion: v1
kind: ResourceQuota
metadata:
  name: ray-quota
  namespace: TENANT_NAMESPACE
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 16Gi
    limits.cpu: "8"
    limits.memory: 32Gi
    requests.nvidia.com/gpu: "1"
    pods: "20"
